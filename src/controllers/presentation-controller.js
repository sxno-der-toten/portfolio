import { presentation } from '../views/presentation.js';

document.addEventListener('DOMContentLoaded', () => {
    document.getElementById('presentation-container').innerHTML = presentation(); });